#include <iostream>

#pragma once

class svgWrite {
    private:

    public:
    static void startSVG(std::string filePath);
    //static void drawCircle(std::string filePath, double x, double y, double r);
    //static void drawRectangle(std::string filePath);
    //static void writeText(std::string filePath);

};